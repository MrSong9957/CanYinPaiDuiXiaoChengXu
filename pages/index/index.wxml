<view class="container">
  <image class="shop-icon" src="/images/logo.png"></image>
  <text class="shop-name">顶楼火盆烧烤</text>
  <button bindtap="handleLogin" class="login-btn">微信登录</button>
</view>

<view class="modal" wx:if="{{showModal}}">
  <view class="modal-content">
    <button class="confirm-btn" bindtap="confirmLogin">确认微信登录</button>
  </view>
</view>